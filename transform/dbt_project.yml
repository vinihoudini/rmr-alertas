name: 'pepluvi'
version: '1.0.0'
config-version: 2

profile: 'pepluvi'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  pepluvi:
    bronze:
      +schema: bronze
    silver:
      +materialized: table
    gold:
      +materialized: view
